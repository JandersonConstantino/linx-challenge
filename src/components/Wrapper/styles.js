import styled from 'styled-components';
import { primaryLight } from 'core/colors';

const Wrapper = styled.div`
  background-color: ${primaryLight};
  width: 100vw;
  height: 100vh;
  padding: 15px;
`;

export default Wrapper;
